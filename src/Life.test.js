import Life from './Life';
const rows = 50;
const cols = 50;
const seed = [[0,0], [0, 1], [1, 0], [1, 3], [2, 1], [2, 2]]; //initialState


describe('Life Engine', () => {
  it('Be able to, per the rules from the Game of Life wiki, take one step through the game of life, updating the cells correctly.', () => {

  });
  it('', () => {

  });

});
